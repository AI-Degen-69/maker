### PLAN: Refactoring Rebate Calculation Logic in `fleet_dash.py`

#### Context & Diagnosis
You confirmed that `rent_reward` is fully wired at `fleet_dash.py:442` using `h["avg_share"] * live_pot * (hours_held / 24)`. However, on the live dashboard, **Earned Rebates / Dividend / Rebate Income evaluates to $0.00** with the subtext *"no rebate-paying position held"*.

This is a logical disconnect:
1. **Live Proof from Polymarket:** The active market (e.g., Valorant) returns a live API `SCORE SHARE` of **37.7%** and `UPTIME` of **24%**, with active limit orders in the order book.
2. **The Cause:** `fleet_dash.py` (lines ~425–436, 442) currently filters out markets tagged internally as `EARNING SPREAD` or markets without an open position from the `rent_reward` sum, treating spread-earning markets as mutually exclusive with rebate collection.
3. **The Reality:** Polymarket's Maker Rebate program pays daily rebates for active liquidity within the qualifying depth regardless of execution status labels or held positions.

---

#### Objectives & Implementation Steps

1. **Remove Internal Status Filtering:**
   - Refactor `fleet_dash.py:442` (and lines ~425–436) so `rent_reward` is **NOT** filtered out based on internal status tags (`EARNING SPREAD`) or open position conditions (`position_held`).
   - Any market with an active API `score_share > 0` (or `avg_share > 0`) **MUST** contribute to `rent_reward`.

2. **Align Rate Resolution with Polymarket Documentation:**
   - Ensure `live_pot` dynamically resolves the correct category rebate percentage based on Polymarket's official Maker Rebate tier table:
     - **Crypto:** 20%
     - **Sports / Esports:** 15%
     - **Politics / Economics / Culture / Tech / Other:** 25%
     - **Geopolitics:** Fee-free (0%)
   - Confirm that `live_pot` does not default or fall back to 0 for active markets.

3. **Trace Runtime Variables (`strategy/store.py`):**
   - Inspect `hist[cid]` aggregation to verify that `hours_held` and `h["avg_share"]` are accurately persisting and passing non-zero values into `fleet_dash.py`.

4. **UI Update & Tracking:**
   - Update the Hero "Earned Rebates" line and the "Dividend / Rebate Income" KPI card to reflect the live accumulated dollar value of Maker Rebates generated by active limit order placement.

Please execute this plan and provide the diffs for `fleet_dash.py`.