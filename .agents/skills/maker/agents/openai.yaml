interface:
  display_name: "Maker"
  short_description: "Repo-specific patterns and workflows for maker"
  default_prompt: "Use the maker repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true